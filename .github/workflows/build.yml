name: Build unsigned IPA

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    # 2026-08 現況:macos-latest = macOS 26,預設 Xcode 26.6。
    runs-on: macos-latest
    # mlx-swift 是整包 vendored C++ 核心 + 上百個 Metal shader,clean build
    # 估計 20-40 分鐘,30 分鐘的舊 timeout 大機率不夠。
    timeout-minutes: 90

    env:
      SCHEME: ImgTranslate
      PROJECT: ImgTranslate.xcodeproj

    steps:
      - uses: actions/checkout@v4

      - name: 環境資訊
        run: |
          sw_vers
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-version

      - name: 安裝 XcodeGen
        run: |
          set -euo pipefail
          if ! command -v xcodegen >/dev/null 2>&1; then
            brew install xcodegen
          fi
          xcodegen --version

      - name: 產生 Xcode 專案
        run: xcodegen generate --spec project.yml

      - name: 解析 SPM 依賴
        run: |
          set -euo pipefail
          xcodebuild -resolvePackageDependencies \
            -project "$PROJECT" -scheme "$SCHEME" \
            -clonedSourcePackagesDirPath SourcePackages
          cat SourcePackages/workspace-state.json | head -60 || true

      - name: 快取 SPM 套件
        uses: actions/cache@v4
        with:
          path: SourcePackages
          key: spm-${{ hashFiles('project.yml') }}

      - name: 快取 DerivedData
        uses: actions/cache@v4
        with:
          path: build
          key: dd-${{ hashFiles('project.yml') }}-${{ github.sha }}
          restore-keys: dd-${{ hashFiles('project.yml') }}-

      - name: 診斷用 build settings
        run: |
          xcodebuild -project "$PROJECT" -scheme "$SCHEME" \
            -configuration Release -sdk iphoneos -showBuildSettings \
            | grep -E 'PRODUCT_NAME|PRODUCT_BUNDLE_IDENTIFIER|BUILT_PRODUCTS_DIR|IPHONEOS_DEPLOYMENT_TARGET|^ *ARCHS|SWIFT_VERSION' || true

      - name: 編譯(unsigned, device)
        run: |
          set -euo pipefail
          xcodebuild \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            -clonedSourcePackagesDirPath SourcePackages \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_ENTITLEMENTS="" \
            ENABLE_BITCODE=NO \
            build

      - name: 打包 IPA(同時產 unsigned 與 ad-hoc 兩顆)
        run: |
          set -euo pipefail
          APP=$(find build/Build/Products -maxdepth 2 -name '*.app' -type d | head -n 1)
          echo "APP=$APP"
          test -n "$APP"
          file "$APP/ImgTranslate" || true
          lipo -info "$APP/ImgTranslate" || true

          rm -rf Payload && mkdir -p Payload
          cp -R "$APP" Payload/
          zip -qry "ImgTranslate-unsigned.ipa" Payload

          rm -rf Payload && mkdir -p Payload
          cp -R "$APP" Payload/
          if codesign --force --sign - --timestamp=none "Payload/$(basename "$APP")"; then
            echo "ad-hoc 簽名成功"
          else
            echo "ad-hoc 簽名失敗,這顆與 unsigned 相同"
          fi
          zip -qry "ImgTranslate-adhoc.ipa" Payload

          ls -lh ImgTranslate-*.ipa
          unzip -l ImgTranslate-unsigned.ipa | head -30

      - name: 驗證 MLX metallib 有被打包
        run: |
          set -euo pipefail
          unzip -l ImgTranslate-unsigned.ipa | grep -i "metallib\|Cmlx" || {
            echo "❌ 找不到 mlx-swift_Cmlx.bundle / default.metallib,MLX 在裝置上一定會爆"
            exit 1
          }

      - uses: actions/upload-artifact@v4
        with:
          name: ImgTranslate-ipa
          path: |
            ImgTranslate-unsigned.ipa
            ImgTranslate-adhoc.ipa
          if-no-files-found: error
          retention-days: 30

      - name: 失敗時上傳 build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: build/Logs
          if-no-files-found: ignore
